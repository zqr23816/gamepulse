FROM python:3.13-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY gamepulse ./gamepulse
RUN mkdir -p /app/data
EXPOSE 8000
CMD ["uvicorn", "gamepulse.main:app", "--host", "0.0.0.0", "--port", "8000"]

