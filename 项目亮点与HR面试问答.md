# GamePulse 项目亮点与 HR 面试问答报告

## 一、项目定位

GamePulse 是一个面向多人在线游戏的遥测、玩家分析与竞技排行榜平台。游戏客户端可以批量上报登录、对局、升级、购买等事件；服务端负责参数校验、身份校验、幂等去重、事务持久化和排名计算；运营人员通过中文数据看板观察在线人数、实时对局、事件吞吐量、API 错误率和排行榜变化。

求职定位优先面向 Python 后端开发、软件开发工程师，同时保留游戏业务背景，能够与游戏服务端、数据平台和运营工具类岗位形成关联。

## 二、项目闪光点

### 1. 不是单纯的可视化页面，而是完整的软件工程项目

项目同时包含 Python/FastAPI 主后端、React 数据看板、SQLite/Cloudflare D1 数据层、Dockerfile、数据库迁移、自动化测试和 GitHub Actions。它能够展示从需求分析、接口设计、领域逻辑、持久化到部署交付的完整能力。

### 2. 用幂等机制解决真实的分布式系统问题

游戏客户端可能因为弱网或请求超时重复上报同一批事件。GamePulse 要求客户端携带 `Idempotency-Key`，并在数据库中保存该请求的处理结果。相同请求再次到达时直接返回第一次的结果，不会重复生成事件。

更重要的是，“检查幂等键、写入事件、保存响应”处于同一事务内，避免两个并发请求都在检查后认为自己是第一次请求。

### 3. 领域逻辑与基础设施分离

Elo 评分计算和稳定事件 ID 生成被实现为无副作用函数，与 FastAPI、HTTP 和数据库解耦。这样既容易测试，也方便未来替换数据库或消息队列，能够体现清晰的代码边界意识。

### 4. 对数据一致性和并发边界有明确认识

SQLite 版本使用 `BEGIN IMMEDIATE` 提前获得写锁，让同一个幂等键下的竞争写入串行化。项目没有把 SQLite 描述成“无限扩展”的生产数据库，而是清楚说明其单写者限制，以及向 Kafka、ClickHouse、PostgreSQL、Redis 演进的路径。

### 5. 具备可验证的工程质量

项目提供领域单元测试和 API 集成测试，覆盖 Elo 零和性、事件 ID 稳定性、批量事件幂等重放和 API 鉴权。GitHub Actions 会在每次提交时自动完成 Python 测试、前端 Lint 和生产构建。

### 6. 有意识地保护公开演示环境

线上看板允许访客模拟对局，但设有每日写入上限；批量事件摄取默认关闭，只有配置服务端密钥后才开放。这体现了“演示可用”和“避免公开数据库被滥用”之间的安全取舍。

### 7. 能够讲清楚技术取舍，而不是堆砌技术名词

当前实现选择 SQLite/D1，是为了让招聘者可以低成本、随时访问演示。项目同时给出了流量扩大后的拆分策略，说明作者既能完成当前交付，也知道架构在什么条件下需要升级。

## 三、简历推荐写法

- 使用 Python、FastAPI、Pydantic 与 SQLite 实现游戏遥测服务，设计批量事件摄取、排行榜查询和健康检查等 REST API，并自动生成 OpenAPI 文档。
- 基于 `Idempotency-Key` 与数据库写事务实现可重试事件上报，避免网络重传导致重复数据；将 Elo 算法抽离为纯领域逻辑并完成单元及接口测试。
- 构建 React 中文数据运营看板与 Cloudflare D1 在线演示，配置 Docker 和 GitHub Actions，实现前后端构建、测试与部署自动化。

不要使用“百万级并发”“生产级高可用”“性能提升 90%”等没有压测或真实业务数据支持的表述。

## 四、HR 常见问题与参考答案

### 1. 请用一分钟介绍这个项目。

GamePulse 是我针对多人游戏场景设计的遥测与排行榜平台。游戏客户端可以批量上报玩家行为，FastAPI 服务负责校验、鉴权和持久化。我重点解决了网络重试可能造成的重复事件问题：通过幂等键和数据库事务保证同一批数据只处理一次。比赛结果会通过 Elo 算法更新排名，并在中文运营看板上展示。项目还包括 Docker、自动化测试、CI 和公开在线 Demo，能够完整展示我从后端设计到部署交付的能力。

### 2. 为什么想做这个项目？

我的本科毕业设计与游戏开发相关，但我希望求职方向更偏软件开发。这个项目把游戏业务理解与通用后端能力结合起来：既有玩家、比赛和排行榜等熟悉的场景，也能展示 API、数据库、事务、测试和部署能力。

### 3. 这个项目是你独立完成的吗？

我负责了需求拆分、接口契约、数据模型、领域逻辑、自动化测试、看板和部署。FastAPI、React 等框架提供基础能力，但幂等策略、事务边界、Elo 计算、接口限制和项目结构是围绕这个业务场景完成的设计。

### 4. 项目中最难的部分是什么？

最难的不是写出一个事件接口，而是确保客户端重试时不会产生重复数据。只在内存中记录请求不可靠，只先查询再写入也存在并发竞争。因此我把幂等检查、事件写入和响应缓存放进同一个数据库写事务中，使结果具有一致性。

### 5. 你在项目中遇到过什么问题，怎么解决的？

一个典型问题是本地测试通过，但最初 CI 环境无法导入 Python 包。原因是本地通过当前工作目录隐式找到模块，而 CI 的执行入口不同。我补充了标准 Python 包配置，并在 CI 中先以可编辑模式安装项目，再通过 `python -m pytest` 运行测试。修复后前端与后端任务都能稳定通过。

### 6. 为什么使用 SQLite，而不是 MySQL 或 PostgreSQL？

这个项目首先要成为招聘者可以直接运行和访问的作品，因此 SQLite/D1 能降低环境和运维成本。它适合演示和中小规模写入，但单写者模型会限制高并发。真实流量上升后，我会把强一致业务状态迁移到 PostgreSQL，排行榜热数据放到 Redis，遥测事件通过 Kafka 异步写入 ClickHouse。

### 7. 如果用户量扩大一千倍，你会怎样改造？

我会分四层处理：第一层在 API 网关完成鉴权、限流和请求大小控制；第二层将事件写入 Kafka，并按游戏或玩家进行分区；第三层由消费者批量写入 ClickHouse，同时把核心业务状态写入 PostgreSQL；第四层用 Redis Sorted Set 提供排行榜热查询。此外还需要死信队列、消费延迟监控、链路追踪和幂等键过期策略。

### 8. 如何证明你的项目质量？

可以从三方面证明：第一，单元测试验证 Elo 和稳定事件 ID；第二，接口测试使用相同幂等键发送两次请求，断言第二次返回重放标记且事件 ID 完全一致；第三，GitHub Actions 在每次提交后执行测试、Lint 和生产构建。在线 Demo 还能直接展示核心交互。

### 9. 你最满意的设计是什么？

我最满意的是将业务不变量放在明确的边界内。幂等逻辑由数据库事务保证，Elo 算法是独立纯函数，API 层只负责协议和校验。这种结构让每一层都更容易测试，也便于未来替换技术组件。

### 10. 如果重新做一次，你会改进什么？

我会优先补充并发压测、结构化日志和 OpenTelemetry 链路追踪；把 API Key 升级为 OAuth2/JWT；为幂等记录增加 TTL；再增加 Kafka 和 Redis 的可选部署方案。这样可以从“可靠的作品级系统”进一步走向可观测、可扩展的服务。

### 11. 这个项目如何体现团队协作能力？

虽然当前版本由我独立实现，但接口契约、模块边界、README、自动化测试和 CI 都是为了降低团队协作成本。真实团队中，客户端可以根据 OpenAPI 对接，前端基于稳定接口开发，代码评审可以依靠自动化检查发现回归。

### 12. 为什么你适合软件开发岗位？

这个项目证明我不仅能完成业务功能，还会考虑重复请求、事务一致性、鉴权、测试、部署和系统演进。我能够从用户需求出发，把问题拆成可维护、可验证、可交付的软件模块，同时愿意明确说明方案的边界和不足。

## 五、技术面试可能继续追问的问题

### 幂等键是否可以永久保存？

不应该。生产环境一般根据客户端最大重试窗口设置 TTL，并定期清理。还要将幂等键与租户、接口和请求摘要绑定，避免同一个键被错误地复用于不同请求。

### `BEGIN IMMEDIATE` 有什么作用？

它在事务开始时就尝试获得 SQLite 的写锁，而不是等到第一条写语句执行时才竞争。这样可以更早地串行化竞争写入，但也会增加锁等待，因此需要设置合理超时，并在更高流量下迁移到更适合并发写入的架构。

### Elo 算法有什么局限？

基础 Elo 更适合一对一、结果明确的比赛，对多人组队、角色差异和新玩家冷启动支持有限。后续可以增加赛季衰减、置信度或改用 Glicko-2、TrueSkill，并通过离线数据评估参数。

### 为什么排行榜不在查询时实时计算？

项目采用增量更新的物化排名投影，查询只需要按评分索引排序，读路径更简单。规模扩大后可以由事件消费者异步更新 Redis Sorted Set，并通过周期性快照校正。

### 怎样防止 API Key 泄露？

密钥只保存在服务端环境变量或密钥管理系统中，不提交到 Git。生产环境应支持密钥轮换、权限范围、审计日志和过期时间；浏览器端不能保存拥有批量写权限的服务端密钥。

## 六、推荐现场演示顺序

1. 打开中文在线仪表盘，介绍四项核心指标。
2. 点击“模拟一场对局”，展示对局数和实时事件流变化。
3. 打开 FastAPI `/docs`，介绍批量事件和排行榜接口。
4. 使用相同幂等键提交两次，展示第二次请求不会重复写入。
5. 展示 `repository.py` 的事务边界和 `domain.py` 的 Elo 纯函数。
6. 展示 GitHub Actions 绿色结果，最后主动说明 SQLite 的限制与演进方案。

## 七、回答原则

- 只讲自己能够解释并在代码中找到证据的内容。
- 优先说明“为什么这样设计”，而不仅是“用了什么技术”。
- 主动承认当前限制，同时给出有条件、有层次的演进方案。
- 不虚构用户量、吞吐量、性能提升或商业效果。

